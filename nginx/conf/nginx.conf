#nginx/cong/nginx.conf
user www-data;
worker_processes auto;
pid /run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    #include mime.types;
    include /etc/nginx/mime.types;

    server {
        listen 80;
        #server_name api.ponggame.com;
        server_name localhost; #api.ponggame.com;

        # ssl_certificate /etc/nginx/ssl/ft_transcendence.crt;
		# ssl_certificate_key /etc/nginx/ssl/ft_transcendence.key;

        # ssl_protocols TLSv1.2 TLSv1.3;
        # ssl_prefer_server_ciphers on;

        access_log /dev/stdout;
        error_log /dev/stdout;
        gzip on;

        # Serve frontend static files
        #location / {
        #    root /usr/share/nginx/html;
        #    index index.html;
        #    try_files $uri $uri/ /index.html;
        #}

        # API proxy to backend microservices
        # location /api/ {
        #     proxy_pass http://auth-service:5500/;
        #     proxy_set_header Host $host;
        #     proxy_set_header X-Real-IP $remote_addr;
        #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        #     proxy_set_header X-Forwarded-Proto $scheme;
        # }

        # location /api/users/ {
        #     proxy_pass http://user-service:5501/;
        #     proxy_set_header Host $host;
        #     proxy_set_header X-Real-IP $remote_addr;
        #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        #     proxy_set_header X-Forwarded-Proto $scheme;
        # }

        # location /api/matchmaking/ {
        #     proxy_pass http://tournament-service:5600/;
        #     proxy_set_header Host $host;
        #     proxy_set_header X-Real-IP $remote_addr;
        #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        #     proxy_set_header X-Forwarded-Proto $scheme;
        # }

        location /api/lobby/ {
            proxy_pass http://lobby:3002/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

    }
}